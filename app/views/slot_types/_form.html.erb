<%= form_with(model: slot_type) do |form| %>
  <% if slot_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(slot_type.errors.count, "error") %> prohibited this slot_type from being saved:</h2>

      <ul>
        <% slot_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
